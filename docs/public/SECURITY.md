# Security policy

Please do not attach real working copies, SVN credentials, repository URLs, or generated merge reports to public issues.

Report a suspected vulnerability privately through GitHub Security Advisories. A public security contact will be added before the first public release.

The application invokes the locally installed Subversion client and uses that client's authentication configuration. Credentials must never be stored in this repository or bundled into release archives.
